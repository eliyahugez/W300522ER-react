import { Button } from "@mui/material"

const FormButton = ({
    variant,
    component,
    size,
    color,
    onClick,
    disabled,
    node
}) => {
    return (
        <Button
            variant={variant}
        >

        </Button>

    )
}